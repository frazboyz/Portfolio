<!DOCTYPE html>
<html>
<head>
  <title>CV</title>
  <%= stylesheet_link_tag 'generic' %>
  <%= stylesheet_link_tag params[:controller] %>
  <%= yield :headers %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="header">
      <a href="#">Francis Malloch-Boe<span>Yes, I'm a Software Engineer</span></a>

      <ul class="menu">
        <li>
          <a class="<%= params[:controller] == 'application' ? 'current' : '' %>" href="<%= url_for :controller => 'application' %>">Overview</a>
        </li>
        <li>
          <a class="<%= params[:controller] == 'skills' ? 'current' : '' %>" href="<%= url_for :controller => 'skills' %>">Skills</a>
        </li>
        <li>
          <a class="<%= params[:controller] == 'projects' ? 'current' : '' %>" href="<%= url_for :controller => 'projects' %>">Projects</a>
        </li>
        <li>
          <a class="<%= params[:controller] == 'hobbies' ? 'current' : '' %>" href="/">Hobbies</a>
        </li>
        <li>
          <a class="<%= params[:controller] == 'blog' ? 'current' : '' %>" href="/">Blog</a>
        </li>
      </ul>
    </div>
    <div class="content">
      <%= yield %>
    </div>
    <div class="footer">
    </div>
  </div>
</body>
</html>

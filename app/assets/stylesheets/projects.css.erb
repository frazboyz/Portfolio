/* Edit/Delete/New button styles */
a.new-project, a.edit-project, a.delete-project {
  position: absolute;
  right: 0;
  top: 3px;
}
a.edit-project {
  right: 120px;
}
.skills > .skill {
  -webkit-box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.2);
  -moz-box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.2);
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.2);
}